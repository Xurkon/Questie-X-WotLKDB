# selene configuration for Questie-X-WotLKDB

std = "wow_classic"

# Questie-specific globals: see skill wow-addon-selene references/
# Custom stdfile: wow_classic_questie.yml (reference only — not auto-loaded by selene)

[lints]
incorrect_standard_library_use = "allow"
unused_variable = "allow"
global_usage = "allow"
duplicate_key = "allow"
ambiguous_if = "allow"
too_many_arguments = "allow"
